import React from 'react'

type Props {
    type : string,
    session : Sessi
}

const ProjectForm = ({type , session} : {type : string , session : }) => {
  return <div></div>
}

export default ProjectForm